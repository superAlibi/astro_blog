---
interface Props {
  currentPage: number;
  totalPages: number;
  basePath?: string; // e.g. '/blog/page'
}

const {
  currentPage,
  totalPages,
  basePath = "/blog/page",
} = Astro.props as Props;
const prevHref =
  currentPage > 1
    ? currentPage === 2
      ? "/blog/"
      : `${basePath}/${currentPage - 1}/`
    : null;
const nextHref =
  currentPage < totalPages ? `${basePath}/${currentPage + 1}/` : null;
---

{
  totalPages > 1 && (
    <nav
      class="mt-6 flex items-center justify-center gap-3"
      aria-label="Pagination"
    >
      {prevHref ? (
        <a href={prevHref} class="sw-btn">
          上一页
        </a>
      ) : (
        <span class="sw-btn" style="opacity:.5; pointer-events:none;">
          上一页
        </span>
      )}
      <span class="text-sm text-[color:var(--color-text-muted)]">
        {currentPage} / {totalPages}
      </span>
      {nextHref ? (
        <a href={nextHref} class="sw-btn">
          下一页
        </a>
      ) : (
        <span class="sw-btn" style="opacity:.5; pointer-events:none;">
          下一页
        </span>
      )}
    </nav>
  )
}
